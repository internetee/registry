- @contact[:disclose].each do |flag, data|
  - next if data.empty?
  .panel.panel-default
    .panel-heading
      %h3.panel-title= t("disclosure-#{flag}")
    .panel-body
      %dl.dl-horizontal
        - data.each do |k,v|
          %dt= t(k)

