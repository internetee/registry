.panel.panel-default
  .panel-heading
    %h3.panel-title= t(:general)
  .panel-body
    %dl.dl-horizontal
      %dt= t(:ident)
      %dd= ident_for(@contact)

      %br

      %dt= t(:email)
      %dd= @contact.email

      %dt= t(:phone)
      %dd= @contact.phone

      %br

      %dt= t(:contact_code)
      %dd= @contact.id

      %dt= t(:password)
      %dd= @contact.password
